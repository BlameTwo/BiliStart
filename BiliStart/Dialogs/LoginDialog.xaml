<ContentDialog
    x:Class="BiliStart.Dialogs.LoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:BiliStart.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    CloseButtonText="关闭"
    Closed="{x:Bind ViewModel.LoginClose}"
    CornerRadius="10"
    mc:Ignorable="d">

    <Pivot Grid.Row="1">
        <PivotItem Header="扫码登陆">
            <Grid HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <Image
                    Width="160"
                    Height="160"
                    Source="{x:Bind ViewModel._QRImage, Mode=OneWay}" />
                <Button
                    Grid.Row="1"
                    Margin="0,10,0,0"
                    HorizontalAlignment="Stretch"
                    Command="{x:Bind ViewModel.RefQrCommand}"
                    Content="刷新"
                    Style="{ThemeResource AccentButtonStyle}" />
            </Grid>
        </PivotItem>
        <PivotItem Header="账号密码登录" />
    </Pivot>
</ContentDialog>
